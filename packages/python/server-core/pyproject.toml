[project]
name = "rtrq-server-core"
version = "0.0.0"
description = "Core server primitives for RTRQ FastAPI deployments."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rtrq_server_core"]
