# Python tooling

Shared Python tooling configuration lives in the root `pyproject.toml`.

Use this directory for future helper scripts, release tooling, or reusable pytest fixtures that apply across
Python packages and applications.
