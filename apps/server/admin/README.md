# RTRQ Server Admin

Standalone admin frontend scaffold for self-hosted RTRQ servers.

The admin UI will let operators create RTRQ apps, view app IDs, rotate API keys, and manage optional browser
origin allowlists for WebSocket connections.

This directory is intentionally minimal until the server implementation begins.
